{
    "name": "akigumo",
    "dockerComposeFile": "../compose.yaml",
    "service": "workspace",
    "workspaceFolder": "/workspace",
    "postCreateCommand": "npm install -g npm@12.0.2 && npm install && cd frontend && npm install && cd ../backend && npm install"
}
